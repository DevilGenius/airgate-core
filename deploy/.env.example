# AirGate 生产环境配置
# 复制此文件为 .env 并修改对应值，docker compose 会自动加载

# 数据库密码（必须修改）
DB_PASSWORD=airgate

# JWT 签名密钥（必须修改，任意字符串即可）
JWT_SECRET=change-me-in-production

# API Key 加密密钥（可选，hex 编码 64 字符，留空使用内置默认值）
# 生成命令: openssl rand -hex 32
# API_KEY_SECRET=

# 服务端口（默认 9517）
# PORT=9517
